# data_merged/

Frozen per-module merged LISS files (`ch_merged.sav`, `ci_merged.sav`,
`cp_merged.sav`) read by the pipeline via `config$merged_dir`. Derived from
the single-wave files in `../data/`; person-level microdata, therefore not
tracked. Merge logs and summaries are available from the authors.
